name: lint

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  shell:
    runs-on: ubuntu-latest      # shellcheck is preinstalled on GitHub runners
    steps:
      - uses: actions/checkout@v4
      - name: Shell syntax + shellcheck + Brewfile groups
        run: ./tools/lint.sh
